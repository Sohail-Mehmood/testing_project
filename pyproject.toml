[project]
name = "testing-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sohail-Mehmood", email = "khan.sb1210@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "openai-agents>=0.0.13",
    "python-dotenv>=1.1.0",
]

[project.scripts]
testing-project = "testing_project:main" 
testing-agent = "testing_project.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
